<% content_for :title, "Edit Post" %>

<div class="editor-container">
  <%= form_with(model: @post, data: { controller: "word-counter autogrow" }) do |form| %>
    <% if @post.errors.any? %>
      <div class="flash alert">
        <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
        <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="editor-header">
      <div class="word-count" data-word-counter-target="counter">0/500</div>
    </div>

    <%= form.text_area :content,
      class: "content-editor",
      data: {
        word_counter_target: "textarea",
        autogrow_target: "textarea",
        action: "input->word-counter#updateWordCount input->autogrow#resize"
      } %>

    <div class="editor-footer">
      <%= form.submit "Update Post", class: "btn" %>
    </div>
  <% end %>
</div>

<div class="actions">
  <%= link_to "Back to Post", @post, class: "btn btn-secondary" %>
  <%= button_to "Delete Post", @post, method: :delete, data: { confirm: "Are you sure you want to delete this post?" }, class: "btn btn-danger" %>
</div>
